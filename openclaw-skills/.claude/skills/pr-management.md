# PR Management Skill

Manage PR lifecycle:
1. Check CI status
2. Apply state labels (draft, ci_running, ci_green, blocked)
3. Request reviews when CI is green
4. Merge when approved and all checks pass
