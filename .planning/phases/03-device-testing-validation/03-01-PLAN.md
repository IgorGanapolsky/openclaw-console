---
phase: 03-device-testing-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "openclaw-skills/package.json",
  "openclaw-skills/src/billing/revenuecat.ts",
  "openclaw-skills/src/analytics/events.ts",
  "openclaw-skills/src/integrations/devops-hub.ts"
]
autonomous: true
requirements: ["TEST-04", "TEST-05"]

must_haves:
  truths:
    - "RevenueCat subscription billing is functional and can process test purchases"
    - "Analytics track conversion events from app install to subscription signup"
    - "DevOps integrations connect to at least 2 major tools (Slack, PagerDuty)"
    - "Skills gateway serves revenue-generating endpoints"
  artifacts:
    - path: "openclaw-skills/src/billing/revenuecat.ts"
      provides: "Cross-platform subscription management"
      exports: ["initializeRevenueCat", "purchaseSubscription", "restorePurchases"]
    - path: "openclaw-skills/src/analytics/events.ts"
      provides: "Conversion funnel tracking"
      exports: ["trackConversionEvent", "identifyUser", "trackRevenue"]
    - path: "openclaw-skills/src/integrations/devops-hub.ts"
      provides: "DevOps tool integrations"
      exports: ["SlackIntegration", "PagerDutyIntegration", "WebhookHandler"]
  key_links:
    - from: "openclaw-skills/src/billing/revenuecat.ts"
      to: "mobile apps"
      via: "HTTP API endpoints"
      pattern: "/api/billing/(subscribe|restore|status)"
    - from: "openclaw-skills/src/analytics/events.ts"
      to: "Firebase Analytics"
      via: "event tracking"
      pattern: "analytics\\.logEvent"
---

<objective>
Implement high-ROI revenue generation infrastructure in the skills gateway to support $100/day target through subscription billing, conversion analytics, and DevOps integrations.

Purpose: Transform OpenClaw Console from a pure technical tool into a revenue-generating SaaS product targeting solo devs and DevOps professionals at $10-20/month Pro subscriptions.
Output: Revenue-ready skills gateway with billing, analytics, and integrations that can be tested end-to-end with mobile clients.
</objective>

<execution_context>
@/Users/ganapolsky_i/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ganapolsky_i/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-device-testing-validation/03-RESEARCH.md
@openclaw-skills/package.json
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement RevenueCat subscription billing infrastructure</name>
  <files>openclaw-skills/src/billing/revenuecat.ts</files>
  <action>
    Create RevenueCat billing service in the skills gateway following research recommendations. Implement:

    - RevenueCat webhook endpoint for subscription events (purchase, renewal, cancellation)
    - API endpoints: /api/billing/subscribe, /api/billing/status, /api/billing/restore
    - User entitlement checking (free vs pro tier features)
    - Subscription state management with Redis/in-memory cache
    - Error handling for failed purchases and network issues

    Use Express.js patterns consistent with existing gateway. Follow research guidance on cross-platform receipt validation and CustomerInfo as single source of truth.

    Install dependencies: Add @revenuecat/purchases-js to package.json if not present.
  </action>
  <verify>
    curl -X POST http://localhost:18789/api/billing/subscribe returns 200
    curl http://localhost:18789/api/billing/status returns user subscription status
    npm test passes for billing module
  </verify>
  <done>
    RevenueCat billing endpoints are functional and can handle subscription lifecycle events. Webhook validation works. Pro feature gating is implemented.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement conversion analytics and revenue tracking</name>
  <files>openclaw-skills/src/analytics/events.ts</files>
  <action>
    Create analytics service for conversion funnel optimization targeting 2-5% conversion rate per research. Implement:

    - Event tracking functions: app_install, account_created, first_approval, subscription_started
    - Revenue event tracking with Firebase Analytics integration
    - User identification and property setting (user_tier, install_source, device_type)
    - Conversion funnel metrics (install → signup → first_use → subscription)
    - A/B testing support for pricing optimization

    Integration points:
    - Firebase Analytics for free tier compatibility
    - Amplitude SDK preparation for advanced funnels
    - Custom event validation and batching

    Follow research patterns for mobile conversion tracking. Export functions that mobile apps can call via WebSocket or HTTP.
  </action>
  <verify>
    Analytics events POST to /api/analytics/track return 200
    Events appear in Firebase Analytics console (if configured)
    Conversion funnel data structure validates correctly
  </verify>
  <done>
    Analytics service captures all conversion events required for $100/day revenue optimization. Revenue tracking works. Funnel data is structured for A/B testing.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build DevOps integrations hub for premium positioning</name>
  <files>openclaw-skills/src/integrations/devops-hub.ts</files>
  <action>
    Create DevOps integrations module to justify Pro subscription pricing and increase user stickiness per research. Implement:

    - Slack integration: OAuth flow, post messages, interactive buttons for approvals
    - PagerDuty integration: incident fetching, acknowledge actions, escalation triggers
    - Generic webhook handler for other DevOps tools (Datadog, monitoring systems)
    - Integration discovery API for mobile apps to show available connections
    - Credential storage with encryption (following project security patterns)

    Architecture:
    - Modular integration pattern (easy to add new tools)
    - Rate limiting and error handling for external APIs
    - Webhook validation and signature verification
    - Integration status monitoring (connected/disconnected states)

    Start with Slack and PagerDuty as research identifies these as highest-value integrations for DevOps professionals.
  </action>
  <verify>
    /api/integrations/available returns list of supported integrations
    /api/integrations/slack/auth initiates OAuth flow successfully
    /api/integrations/pagerduty/incidents returns test data
    Integration webhook endpoints validate signatures correctly
  </verify>
  <done>
    DevOps integrations provide clear premium value proposition. Slack and PagerDuty integrations are functional. Mobile apps can discover and configure integrations. Webhook security is implemented.
  </done>
</task>

</tasks>

<verification>
Revenue infrastructure is ready for end-to-end testing:
1. Start skills gateway: cd openclaw-skills && npm run dev
2. Test billing endpoints with curl commands
3. Verify analytics events are captured and structured correctly
4. Confirm DevOps integrations authenticate and fetch data
5. Check that premium features are properly gated behind subscription status
</verification>

<success_criteria>
OpenClaw Console has revenue generation infrastructure that supports the $100/day target through:
- Functional subscription billing via RevenueCat integration
- Comprehensive conversion analytics for optimization
- High-value DevOps integrations that justify Pro pricing
- Premium feature gating based on subscription status
- End-to-end testable from mobile apps through skills gateway
</success_criteria>

<output>
After completion, create `.planning/phases/03-device-testing-validation/03-01-SUMMARY.md`
</output>