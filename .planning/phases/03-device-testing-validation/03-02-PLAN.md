---
phase: 03-device-testing-validation
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [
  "android/app/build.gradle",
  "android/app/src/main/java/com/openclaw/console/billing/SubscriptionManager.kt",
  "ios/OpenClawConsole/OpenClawConsole/Services/SubscriptionService.swift",
  "ios/OpenClawConsole/OpenClawConsole/Views/SubscriptionView.swift"
]
autonomous: true
requirements: ["TEST-01", "TEST-02", "TEST-03"]

must_haves:
  truths:
    - "Android app integrates RevenueCat SDK and can process test subscriptions"
    - "iOS app integrates RevenueCat SDK and shows StoreKit purchase UI"
    - "Both apps validate subscription status and gate Pro features"
    - "Biometric approval workflow includes subscription tier validation"
  artifacts:
    - path: "android/app/src/main/java/com/openclaw/console/billing/SubscriptionManager.kt"
      provides: "Android subscription management"
      exports: ["initializePurchases", "purchasePackage", "checkEntitlements"]
    - path: "ios/OpenClawConsole/OpenClawConsole/Services/SubscriptionService.swift"
      provides: "iOS subscription management"
      exports: ["SubscriptionService", "PurchaseManager", "EntitlementChecker"]
    - path: "ios/OpenClawConsole/OpenClawConsole/Views/SubscriptionView.swift"
      provides: "iOS subscription UI"
      exports: ["SubscriptionView", "PaywallView", "SubscriptionStatusView"]
  key_links:
    - from: "SubscriptionManager.kt"
      to: "RevenueCat SDK"
      via: "Purchases.configure"
      pattern: "Purchases\\.configure"
    - from: "SubscriptionService.swift"
      to: "RevenueCat SDK"
      via: "Purchases.configure"
      pattern: "Purchases\\.configure"
    - from: "BiometricService"
      to: "SubscriptionService"
      via: "entitlement check"
      pattern: "checkEntitlements.*biometric"
---

<objective>
Implement client-side subscription billing in both mobile apps to enable revenue generation through Pro tier subscriptions, while validating core device functionality and biometric workflows.

Purpose: Create the client-side revenue infrastructure that works with the skills gateway billing system, allowing real subscription purchases and Pro feature access validation on physical devices.
Output: Revenue-ready mobile apps that can process subscriptions and validate Pro features with biometric security.
</objective>

<execution_context>
@/Users/ganapolsky_i/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ganapolsky_i/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-device-testing-validation/03-RESEARCH.md
@.planning/phases/03-device-testing-validation/03-01-SUMMARY.md
@android/app/build.gradle
@ios/OpenClawConsole/OpenClawConsole.xcodeproj/project.pbxproj
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate RevenueCat SDK in Android app</name>
  <files>android/app/build.gradle</files>
  <action>
    Add RevenueCat Android SDK to the Kotlin/Compose app following research recommendations. Implementation:

    1. Add RevenueCat dependency to build.gradle: `implementation 'com.revenuecat.purchases:purchases:7.+'`
    2. Create SubscriptionManager.kt in billing package with:
       - Purchases.configure() initialization with API keys
       - purchasePackage() method for Pro subscription
       - checkEntitlements() method for Pro feature gating
       - restorePurchases() for account recovery
       - CustomerInfo listener for subscription state changes

    3. Integration with existing app:
       - Initialize in Application onCreate()
       - Connect to biometric approval workflow (Pro features require subscription)
       - Add subscription status to user preferences/state management
       - Handle purchase errors and network failures gracefully

    Follow Android architecture rules: MVVM + Repository pattern, EncryptedSharedPreferences for sensitive data, Hilt dependency injection if applicable.

    Ensure compatibility with existing Kotlin 2.x + Compose setup from Phase 1-2.
  </action>
  <verify>
    ./gradlew assembleDebug builds successfully with RevenueCat integrated
    Android Studio shows no RevenueCat integration warnings
    SubscriptionManager.kt compiles and follows Kotlin coding conventions
  </verify>
  <done>
    Android app has functional RevenueCat integration. Subscription purchase flow is implemented. Pro feature gating works. No build errors or dependency conflicts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate RevenueCat SDK in iOS app</name>
  <files>ios/OpenClawConsole/OpenClawConsole/Services/SubscriptionService.swift</files>
  <action>
    Add RevenueCat iOS SDK to the SwiftUI app following research recommendations and iOS architecture rules. Implementation:

    1. Add RevenueCat to Xcode project via SPM: https://github.com/RevenueCat/purchases-ios
    2. Create SubscriptionService.swift with:
       - Purchases.configure() initialization with API keys
       - purchasePackage() async method for Pro subscription
       - checkEntitlements() method returning subscription status
       - restorePurchases() for App Store account recovery
       - CustomerInfo updates via Combine publishers

    3. Integration with existing SwiftUI + @Observable pattern:
       - Initialize in App.swift or AppDelegate equivalent
       - Connect to BiometricService for Pro feature validation
       - Update user state management with subscription status
       - Handle StoreKit errors and network failures with proper user feedback

    Follow iOS architecture rules: SwiftUI + @Observable, Services layer, async/await patterns, LocalAuthentication integration.

    Ensure compatibility with iOS 17.0+ target and existing project structure.
  </action>
  <verify>
    xcodebuild -scheme OpenClawConsole build succeeds with RevenueCat
    No SPM dependency resolution errors in Xcode
    SubscriptionService.swift follows Swift 6 + SwiftUI patterns
  </verify>
  <done>
    iOS app has functional RevenueCat integration. StoreKit purchase flow works. Pro features are properly gated. Swift compilation succeeds without warnings.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create subscription UI and biometric Pro feature validation</name>
  <files>ios/OpenClawConsole/OpenClawConsole/Views/SubscriptionView.swift</files>
  <action>
    Build subscription management UI for iOS and integrate with biometric approval workflow. Implementation:

    1. Create SubscriptionView.swift with SwiftUI:
       - Paywall view showing Free vs Pro feature comparison
       - Purchase button triggering RevenueCat subscription flow
       - Subscription status indicator (Free/Pro/Expired)
       - Restore purchases option for existing subscribers
       - Loading states and error handling for purchase flows

    2. Integrate with biometric approval workflow:
       - Check subscription entitlements before allowing biometric actions
       - Show upgrade prompt for Pro-only features (advanced approval types)
       - Validate subscription status on app launch and resume
       - Cache subscription state for offline scenarios

    3. Follow design system and UX patterns:
       - Material Design 3 for Android (if time permits, focus on iOS first)
       - SwiftUI native controls and animations
       - Accessibility support for subscription UI
       - Clear value proposition for Pro features

    Connect to SubscriptionService from Task 2. Ensure biometric workflows respect subscription tier (basic approvals for free, advanced for Pro).
  </action>
  <verify>
    Subscription UI displays correctly in iOS Simulator
    Purchase flow can be triggered (sandbox testing)
    Biometric approval respects subscription tier
    UI follows SwiftUI best practices and accessibility guidelines
  </verify>
  <done>
    Subscription UI provides clear upgrade path. Biometric workflows validate subscription tier. Pro features are properly gated. Purchase flow is user-friendly and functional.
  </done>
</task>

</tasks>

<verification>
Mobile apps are revenue-ready and device-testable:
1. Build both apps: ./gradlew assembleDebug && xcodebuild -scheme OpenClawConsole build
2. Install on test devices and verify subscription purchase flows work
3. Test biometric approval with different subscription tiers
4. Verify Pro features are gated correctly
5. Confirm apps connect to skills gateway and respect subscription status
</verification>

<success_criteria>
Both mobile apps support revenue generation through Pro subscriptions:
- RevenueCat SDK integrated and functional on iOS and Android
- Subscription purchase flows work on physical devices
- Biometric approval workflow validates subscription tier
- Pro features are properly gated and provide clear value
- Apps can be tested end-to-end with revenue infrastructure
</success_criteria>

<output>
After completion, create `.planning/phases/03-device-testing-validation/03-02-SUMMARY.md`
</output>