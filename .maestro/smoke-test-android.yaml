appId: com.openclaw.console
---
# Smoke test: Verify app launches on Android
- launchApp

# Optional sanity checks (UI can vary by connection/bootstrap state in CI)
- assertVisible:
    text: '.*(Agents|Settings|Connect|Gateway).*'
    optional: true
