appId: com.openclaw.console
---
# Smoke test: Verify app launches on iOS
- launchApp

# Verify main screen elements
- assertVisible: 'OpenClaw Console'
- assertVisible:
    text: '.*Connect.*'
    optional: true
