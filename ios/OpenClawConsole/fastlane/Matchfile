git_url(ENV["MATCH_GIT_URL"])
storage_mode("git")
type("appstore")
app_identifier("com.openclaw.console")

# Use App Store Connect API key authentication instead of Apple ID
# Don't set username to force API key authentication when available
